let int a = '123';
let boolean bool = 10;
let int num;

function auxiliar int (int a, int b, int c, int d, int e){
    return a;
}

num = auxiliar(a,12);
num = auxiliar(num, 'esto','esta','mal', bool);

function factorial int (int n){         // parámetro n igual que variable global
let int n = 10;                     // provoca error ya // que ya existe n en la función (ámbito actual)
    return n;
}
