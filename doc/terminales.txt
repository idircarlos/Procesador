Definición del significado de los terminales
Se asignó al terminal ! la representación Negación(!)

Se asignó al terminal %= la representación Asignación con módulo(%=)

Se asignó al terminal ( la representación Paréntesis abierto(()

Se asignó al terminal ) la representación Paréntesis cerrado())

Se asignó al terminal , la representación Coma(,)

Se asignó al terminal - la representación Resta(-)

Se asignó al terminal ; la representación Punto y coma(;)

Se asignó al terminal < la representación Menor(<)

Se asignó al terminal = la representación Asignación(=)

Se asignó al terminal boolean la representación boolean(boolean)

Se asignó al terminal cadena la representación Cadena(Cadena)

Se asignó al terminal entero la representación Número(Numero)

Se asignó al terminal for la representación for(for)

Se asignó al terminal function la representación function(function)

Se asignó al terminal id la representación Identificador(Identificador)

Se asignó al terminal if la representación if(if)

Se asignó al terminal input la representación input(input)

Se asignó al terminal int la representación int(int)

Se asignó al terminal let la representación let(let)

Se asignó al terminal print la representación print(print)

Se asignó al terminal return la representación return(return)

Se asignó al terminal string la representación string(string)

Se asignó al terminal { la representación Llave abierta({)

Se asignó al terminal } la representación Llave cerrada(})

Datos almacenados con éxito.